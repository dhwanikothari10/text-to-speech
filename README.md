# 📰 AI News Reader (Streamlit App)

This project fetches the latest news on a user-specified topic and reads it aloud using Google's Text-to-Speech (gTTS).  
Built with *Streamlit, **LangChain, and **Google Generative AI*.

## 🚀 Features
- Enter any topic to fetch latest news
- Listen to news with AI voice
- Simple and interactive Streamlit UI

## ⚙️ Setup
1. Clone the repository
2. Create .env file and add your API keys:
GOOGLE_API_KEY = API KEY HERE
NEWS_API_KEY = API KEY HERE
3. install dependencies: pip install -r requirements.txt
4. run the app: streamlit run news.py
